import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// ReactDOM.render 把App组件挂在到某一个dom下
// 挂在到id为root的dom节点下（index.html里）
// ReactDOM.render(
//   // React.StrictMode 开启严格模式
//   // <React.StrictMode>

//   // <App /> 为jsx语法，使用这种语法必须在当前文件中引入react，无论有没有使用到
//   // 定义的组件必须以大写字母开头
//   <App />,
//   // </React.StrictMode>
//   document.getElementById("root")
// );
